import {
  Card,
  CardMedia,
  CardContent,
  Typography,
  Button,
  CardActions,
} from "@material-ui/core";
import styled, { css } from "styled-components";

export const StyledCard = styled(Card)`
  max-width: 20vw;
  margin: 20px;
`;
